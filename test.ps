# Bubble Sort Implementation

Let arr be [6, 2, 9, 1, 5, 6] as array
Let n be arr.Length as number
Let i as number
Let j as number
Let temp as number
Print("Before: ")
Print(arr)
Print("\n")

# Outer loop: Traverse all elements
For (i = 0; i < n - 1; i = i + 1) {
    # Inner loop: Compare adjacent elements
    For (j = 0; j < n - i - 1; j = j + 1) {
        # If the element is greater than the next one, swap them
        If (arr[j] > arr[j + 1]) {
            # Swap arr[j] and arr[j + 1]
            temp = arr[j]
            arr[j] = arr[j + 1]
            arr[j + 1] = temp
        }
    }
}

# Print the sorted array
Print("After: " + arr)
Print("\n")
